@model List<Cinema.Domain.DomainModels.Ticket>



<div class="container-fluid">
    @for (int i = 0; i < Model.Count; i++)
    {
        var item = Model[i];
        if (i % 3 == 0)
        {
            @: <div class="row">
            }

            <div class="col-md-3 m-4">
                <div class="card" style="width:18rem; height:15rem;">
                    <div class="card-body">
                        <h3 class="card-title">@item.Title</h3>
                        <h6 class="card-text">Price: @item.TicketPrice</h6>
                        <p class="card-text">Date: @item.Date</p>
                    </div>

                    <div class="card-footer mx-auto">
                        <a asp-controller="Tickets" asp-action="AddTicketToCard" asp-route-id="@item.Id" class="btn btn-outline-info">Add to Card</a>
                        <a asp-controller="Tickets" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-secondary">Edit</a>

                    </div>


                </div>

            </div>

        }
</div>
</div>